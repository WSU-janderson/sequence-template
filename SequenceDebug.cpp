/**
 * SequenceDebug.cpp
 * Project 1
 * CS 3100
 *
 * In this file, you will write your tests as you implement Sequence. If you are using CLion, you need to select
 * SequenceDebug from the drop-down menu next to the Build (hammer icon) if it is on SequenceTestHarness
 *
 * You **will not** be able to test just the linked list functions here. You can either test them through the Sequence
 * class, or write and test them in the linked list assignment. When you have the functions working, you can then
 * replace linked_list.h in this repo with the fully-tested version. Note: there are three extra functions to implement
 * in linked_list.h: get_node_at(), insert_at(), and erase_at()
 */
#include <iostream>

#include "Sequence.h"



using namespace std;

int main() {
    cout << "Your project is ready to go!" << endl;

    return 0;
}
